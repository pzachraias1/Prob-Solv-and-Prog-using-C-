#include <cstdio>
#include <cmath>
#include "function.h"


int main(int argc, const char * argv[]) {
    
    double angle = 0;
    
    while (angle <= 90){
        printf("When the angle is %.2f --> %.2f meters \n",angle, range(angle));
        angle ++;
    }
    
}
