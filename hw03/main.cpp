#include <cstdio>
#include <cmath>
#include "formula.h"
#include "range.h"


int main(int argc, const char * argv[]) {
    
    double angle = 0;
    
    //double max = getMaxH();
    
    while (angle <= 90){
        distHeight(angle);
        angle ++;
    }
    
}
